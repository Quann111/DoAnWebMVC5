@model IEnumerable<WebApplication1.Areas.Admin.Models.LoaiBaiViet>

@{
    ViewBag.Title = "DanhsachLoaiBV";
    Layout = "~/Areas/Admin/Views/Shared/template.cshtml";
}


<p>
    @Html.ActionLink("Create New", "Them_loai")
</p>
<table class="table">

    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenLoai)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.ID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TenLoai)
    </td>
    <td>
        @Html.ActionLink("Edit", "Update_KhoaHoc", new { id = item.ID }) |
        @*@Html.ActionLink("Details", "Details", new { id=item.ID }) |*@
        @Html.ActionLink("Delete", "XoaTrangSuc", new { id = item.ID })
    </td>
</tr>
    }

</table>
